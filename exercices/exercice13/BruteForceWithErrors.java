package exercices.exercice13;

public class BruteForceWithErrors {

    // Erreurs : pas de PASSWORD, pas de CHARACTERS, pas de Base64

    public static void main(String[] args) {
        System.out.println("BruteForceWithErrors ici !");
        // Pas d'algorithme brute-force, juste une boucle qui ne fait rien
        for (int i = 0; i < 10; i++) {
            System.out.println("Essai : " + i);
        }
    }

    // testPassword manquant
}
